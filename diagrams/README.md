# Milestone Diagrams

Generated diagram sources and rendered SVGs for the current AXI4-Lite design:

- `slave_datapath.dot` / `slave_datapath.svg`
- `top_slave_only_datapath.dot` / `top_slave_only_datapath.svg`
- `slave_fsm.dot` / `slave_fsm.svg`
- `top_slave_only_fsm.dot` / `top_slave_only_fsm.svg`

Render command:

```sh
dot -Tsvg diagrams/<name>.dot -o diagrams/<name>.svg
```
